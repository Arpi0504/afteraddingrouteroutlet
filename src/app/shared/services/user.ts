export interface User {
    uid: string;
    email: string;
    emailVerified: boolean;
    // first_name: string;
    // last_name: string;
    // contact_no: number;
    // project_name: string;
    // displayName: string;
    // photoURL: string;
}