export interface UserService {
    uid: string;
    first_name: string;
    last_name: string;
    contact_no: number;
    project_name: string;
    // $key: string;
    // publication_date: Date;
    // binding_type: string;
    // in_stock: string;
    // languages: Array<string>;
}
